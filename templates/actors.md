<!-- Template for Stage 00 (00_actor-goal). Purpose: see methodology/implementation/STAGES.md §"Stage 00 — Actor / Goal". -->

# Actors

> One row per actor. Keep it short — actors are reference points, not
> personas. Anything that does not name a participant in the system
> belongs in `goals.md` instead.

| Name | Role | Primary concerns |
|---|---|---|
| `<ActorName>` | <one-line role> | <what this actor cares about; comma-separated> |
| `<ActorName>` | <one-line role> | <what this actor cares about> |

## Notes

> Optional. Edge cases, actors considered and rejected, scope boundary
> questions for the human reviewer.
