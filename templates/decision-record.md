# DR-NNNN — <short title>

> Replace `NNNN` with the next free four-digit number under
> `docs/decisions/`. Replace `<short title>` with a noun phrase
> (e.g. "Jena over Postgres for reference profile").

**Status:** Proposed | Accepted | Superseded by DR-NNNN | Deprecated

**Date:** YYYY-MM-DD

## Context

What is the problem or forcing function? Keep this to one short
paragraph. Link to the feature stage that surfaced it, if
applicable. Do not re-derive the problem from first principles —
assume the reader knows the project.

## Decision

What did we choose? One or two sentences. Name the alternative we
rejected so the reader knows this was a choice, not a default.

## Consequences

What becomes easier as a result? What becomes harder? What follow-on
decisions does this create? Three to six bullets is typical.

- ...
- ...

## References

(Optional.) Links to PRs, issues, prior ADRs (especially the one
this supersedes if any), external papers.
