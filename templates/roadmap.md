<!-- Roadmap template. Purpose: see methodology/overlays/TRACKING.md §"ROADMAP convention". -->

# Roadmap

> One row per phase. Exactly one phase has status `doing` at any time;
> everything else is `queued` or `done`. The `doing` phase corresponds
> to the active feature folder under `features/`.

## Phases

| # | Phase | Feature(s) | Status | Notes |
|---|---|---|---|---|
| 1 | <phase name> | `UC-XX-<slug>` | done | <one line> |
| 2 | <phase name> | `UC-XX-<slug>` | doing | <one line> |
| 3 | <phase name> | `UC-XX-<slug>` | queued | <one line> |

## Backlog

> Features identified but not yet promoted into a phase.

- `UC-XX-<slug>` — <one-line summary>
- `UC-XX-<slug>` — <one-line summary>

## Resume point

> Updated at the end of every working session. See
> `methodology/overlays/TRACKING.md` §"Resume-point comment".

- **Last gate passed:** `<UC-XX>` stage `<NN>` on `<date>`
- **Next stage:** `<UC-XX>` stage `<NN>`
- **Blockers:** `<none | list>`
