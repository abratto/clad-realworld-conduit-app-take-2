<!-- Template for Stage 00 (00_actor-goal). Purpose: see methodology/implementation/STAGES.md §"Stage 00 — Actor / Goal". -->

# Goals

> One row per goal. Keep `Goal` as a short, stable phrase and put the
> user-facing reason or further description in `Rationale`. The
> rationale matters — it is what stage 1's use case turns into the
> operational principle.

| Actor | Goal | Rationale | Priority | In scope? |
|---|---|---|---|---|
| `<ActorName>` | `<ShortGoalName>` | to <do X> so that <reason> | P0 \| P1 \| P2 | yes \| no |
| `<ActorName>` | `<AnotherShortGoalName>` | to <do Y> so that <reason> | P0 \| P1 \| P2 | yes \| no |

## Out of scope

> Explicit list of goals that came up but are deliberately excluded.
> Restate the reason briefly.

- `<ActorName>` — `<ShortGoalName>`: out of scope because <reason>.
