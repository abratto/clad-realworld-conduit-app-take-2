# Stage 03a output

This folder is populated by Stage 03a. Per-feature outputs:

- `<concept>-card.md` — one dependency review card per concept.
- `pattern-d-summary.md` — consolidated Pattern D table for the
  feature.

See the templates under `templates/` and the stage `CONTEXT.md`.
