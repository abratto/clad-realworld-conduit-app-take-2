# Comment — dep review
## Invocations: add (via `WhenSessionLookupFoundThenCommentAddForComment`), authorCheck (via `WhenSessionLookupFoundThenCommentAuthorCheckForComment`), delete (via `WhenCommentAuthorCheckIsAuthorThenCommentDeleteForComment`)
## Inbound Pattern D: None
